# ⚽ 2D 平面足球对战游戏 — 开发计划

## 项目概述

一个基于 **原生 TypeScript + Canvas** 的 2D 平面足球对战游戏。
本地双人同屏对战，未来通过 Node.js 后端扩展在线联机。

---

## 🏗️ 技术架构

### 整体架构
```
Phase 1~4: 纯前端（本地对战）
┌──────────────────────────────────────┐
│            Browser                    │
│  ┌────────────────────────────────┐  │
│  │     TypeScript + Canvas        │  │
│  │  ┌──────────┐ ┌────────────┐  │  │
│  │  │ Game     │ │ UI Layer   │  │  │
│  │  │ Engine   │ │ (Menu/HUD) │  │  │
│  │  └──────────┘ └────────────┘  │  │
│  └────────────────────────────────┘  │
└──────────────────────────────────────┘

Phase 5+: 加入后端（在线联机）
┌──────────────┐    WebSocket    ┌──────────────┐
│   Frontend   │ ◄─────────────► │   Backend    │
│   (TS+Canvas)│                 │  (Node.js)   │
└──────────────┘                 └──────────────┘
```

### 前端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| TypeScript | 5.x | 类型安全，减少运行时 Bug |
| HTML5 Canvas | - | 游戏画面渲染（60FPS） |
| Vite | 5.x | 开发服务器 & 构建工具 |
| 原生 JS API | - | 键盘输入、游戏循环 |
| localStorage | - | 持久化用户设置（胜利分数等） |

> **为什么不用 React？**
> 游戏画面每帧重绘，React 的 Virtual DOM 反而是性能负担。
> Canvas 直接操作像素，不需要 DOM 框架。
> UI 部分（菜单、计分板）用简单的 DOM 操作即可。

### 后端技术栈（Phase 5 扩展）
| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | 20.x | 运行时 |
| Express | 4.x | HTTP 服务 |
| ws / Socket.IO | - | WebSocket 实时通信 |

---

## 📁 项目结构

```
football-game/
├── src/                          # 前端源码
│   ├── main.ts                   # 入口文件
│   ├── game/                     # 游戏核心
│   │   ├── Game.ts               # 游戏主类（管理整个游戏生命周期）
│   │   ├── GameLoop.ts           # 游戏循环（requestAnimationFrame）
│   │   ├── Field.ts              # 场地绘制（草坪、线条、球门）
│   │   ├── Player.ts             # 玩家类（位置、速度、绘制）
│   │   ├── Ball.ts               # 足球类（位置、速度、物理）
│   │   ├── Physics.ts            # 物理系统（摩擦、反弹、速度衰减）
│   │   │   │   ├── Collision.ts          # 碰撞检测（圆-圆、圆-矩形）
│   │   ├── InputManager.ts       # 键盘输入管理（多键同时追踪）
│   │   ├── ScoreManager.ts       # 计分 & 胜利判定
│   │   └── constants.ts          # 游戏常量（场地尺寸、速度、颜色等）
│   ├── ui/                       # UI 层（DOM 操作）
│   │   ├── MenuScreen.ts         # 开始菜单
│   │   ├── HUD.ts                # 比分显示 & 游戏信息
│   │   └── GameOverScreen.ts     # 游戏结束画面
│   ├── types/                    # TypeScript 类型
│   │   └── index.ts              # 所有类型定义
│   └── styles/                   # 样式
│       └── style.css             # 全局样式
├── server/                       # 后端（Phase 5）
│   ├── index.ts                  # 服务入口
│   ├── GameRoom.ts               # 房间管理
│   └── WebSocketHandler.ts       # WS 消息处理
├── index.html                    # HTML 入口
├── package.json
├── tsconfig.json
├── vite.config.ts
└── PLAN.md                       # 本文件
```

---

## 🎮 游戏功能设计

### 游戏模式
| 模式 | 说明 | 优先级 |
|------|------|--------|
| 本地对战 | 同屏双人，键盘分控 | ⭐ P0 — 首先实现 |
| 在线对战 | WebSocket 联机 | ⭐ P2 — 后续扩展 |
| 人机对战 | AI 控制对手 | ⭐ P3 — 后续扩展 |

### 操控方案

**玩家 1（左侧 - 红队 🔴）**
| 操作 | 按键 |
|------|------|
| 上移 | `W` |
| 下移 | `S` |
| 左移 | `A` |
| 右移 | `D` |
| 踢球 | `Space` |

**玩家 2（右侧 - 蓝队 🔵）**
| 操作 | 按键 |
|------|------|
| 上移 | `↑` |
| 下移 | `↓` |
| 左移 | `←` |
| 右移 | `→` |
| 踢球 | `Enter` |

### 游戏规则
- 进球后球重置到场地中心，双方回到初始位置
- 胜利分数可配置：**3分 / 5分 / 10分 / 无限**（默认5分）
- 球员不能跑出场地边界
- 足球碰边界反弹，进入球门区域判定进球
- 踢球有冷却时间（0.3 秒），防止连续粘球

### 带球 & 踢球机制
- **自然带球**：球员跑动碰到球时，球被自然推动（物理碰撞），无需按键
- **主动踢球**：按踢球键，球获得冲量，力度根据球员当前跑动速度自动决定
  - 球员静止踢球 → 普通力度
  - 球员跑动踢球 → 更大力度（跑动冲量叠加）
- 带球时球不在身上固定吸附，对方可以抢断

---

## 🧩 核心模块设计

### 1. 游戏主类 (Game.ts)
```typescript
class Game {
  canvas: HTMLCanvasElement
  ctx: CanvasRenderingContext2D
  field: Field
  players: [Player, Player]
  ball: Ball
  scoreManager: ScoreManager
  inputManager: InputManager
  state: 'menu' | 'playing' | 'paused' | 'goal' | 'gameover'
  winScore: number  // 可配置的胜利分数

  init()          // 初始化游戏
  update(dt)      // 更新逻辑（物理、碰撞、输入）
  render()        // 渲染画面
  reset()         // 重置场地
  onGoal(team)    // 进球回调
  setWinScore(score) // 设置胜利分数
}
```

### 玩家类 (Player.ts)
```typescript
class Player {
  x: number               // X 坐标
  y: number               // Y 坐标
  vx: number              // X 速度
  vy: number              // Y 速度
  radius: number          // 半径
  color: string           // 队伍颜色
  direction: number       // 朝向角度（弧度）
  trail: {x, y, opacity}[] // 拖尾位置记录
  kickCooldown: number    // 踢球冷却计时器

  update(dt)              // 更新位置、朝向、拖尾
  render(ctx)             // 绘制（身体 + 箭头 + 拖尾）
  drawBody(ctx)           // 绘制圆形身体
  drawArrow(ctx)          // 绘制朝向箭头
  drawTrail(ctx)          // 绘制跑动拖尾
  updateDirection()       // 根据速度方向更新朝向
  kick(ball)              // 踢球（力度 = 基础力量 + 跑动速度 × 加成系数）
  canKick(): boolean      // 检查踢球冷却
}
```

### 2. 游戏循环 (GameLoop.ts)
```
requestAnimationFrame 驱动
├── 计算 deltaTime
├── update(dt)
│   ├── 处理输入 → 更新玩家速度
│   ├── 物理更新 → 位置 += 速度 * dt
│   ├── 碰撞检测 → 球员-球、球-边界、球-球门
│   └── 碰撞响应 → 反弹、踢球
└── render()
    ├── 绘制场地（草坪、线条、中圈、球门）
    ├── 绘制球员（拖尾 + 圆形身体 + 朝向箭头）
    ├── 绘制足球
    └── 绘制 HUD（比分、时间）
```

### 3. 物理系统 (Physics.ts)
```
物理规则:
├── 玩家移动: 按键方向 × 移动速度
├── 足球摩擦: 每帧速度 *= 摩擦系数 (0.98)
├── 边界反弹: 碰到边线速度取反
├── 自然带球: 球员跑动碰到球 → 球被自然推动（物理碰撞）
├── 主动踢球: 按踢球键 → 球获得跑动速度叠加的冲量（跑得越快踢得越远）
└── 球员碰撞: 圆形碰撞后互相推开
```

### 4. 碰撞检测 (Collision.ts)
```
碰撞类型:
├── circleVsCircle(a, b)     → 球员-球、球员-球员
├── circleVsRect(circle, rect) → 球-边界墙
└── isInsideGoal(ball, goal)  → 进球判定
```

### 5. 输入管理 (InputManager.ts)
```
按键追踪:
├── Set<string> pressedKeys   → 当前按下的所有键
├── onKeyDown / onKeyUp       → 事件监听
├── isPressed(key): boolean   → 查询按键状态
└── 支持多键同时按下（斜向移动）
```

### 6. 设置管理 (SettingsManager.ts)
```
用户设置:
├── winScore: number          → 胜利分数（3/5/10/无限）
├── gameMode: 'score' | 'time' → 游戏模式
├── loadSettings()            → 从 localStorage 加载
├── saveSettings()            → 保存到 localStorage
└── getWinScore(): number     → 获取当前胜利分数
```

### 7. 球员视觉效果
```
绘制层次（从下到上）:
├── 拖尾 (trail)    → 队伍颜色半透明，长度随速度变化
├── 身体 (body)     → 圆形，队伍颜色填充，白色描边
└── 箭头 (arrow)    → 白色三角，位于身体顶部，指向移动方向
```

---

## 🎨 视觉设计

### 场地布局
```
          ┌─────────────────────────────────────┐
          │         │                   │        │
          │  ┌───┐  │                   │ ┌───┐  │
          │  │   │  │                   │ │   │  │
          │  │🥅 │  │                   │ │ 🥅│  │
          │  │   │  │        ⚽         │ │   │  │
          │  └───┘  │                   │ └───┘  │
          │         │                   │        │
          └─────────────────────────────────────┘
                 P1 🔴               P2 🔵
                 WASD               方向键
                 Space              Enter
```

### 颜色方案
| 元素 | 颜色 | 色值 |
|------|------|------|
| 草坪（深） | 深绿 | `#2E7D32` |
| 草坪（浅） | 浅绿 | `#388E3C` |
| 线条 | 白色 | `#FFFFFF` |
| 球员 1 | 红色 | `#F44336` |
| 球员 2 | 蓝色 | `#2196F3` |
| 朝向箭头 | 白色 | `#FFFFFF` |
| 拖尾 | 队伍颜色 | 透明度 0.4 |
| 足球 | 白色 | `#FFFFFF` |
| 球门 | 金色 | `#FFC107` |
| 背景 | 深色 | `#1A1A2E` |

### 视觉细节
- 草坪用交替深浅绿条纹（仿真草坪效果）
- 球员：圆形身体 + 顶部朝向箭头 + 跑动拖尾效果
  - 箭头跟随移动方向旋转，指示球员面朝方向
  - 静止无拖尾，跑越快拖尾越长，拖尾颜色与球队颜色一致
- 球员带编号标识（1P / 2P）
- 足球带黑白纹理旋转效果
- 进球时有闪光/粒子效果

---

## 📅 开发计划

### Phase 1 — 项目搭建 & 场地 ⏳
**目标**: 能在浏览器里看到一个足球场
- [ ] 初始化 Vite + TypeScript 项目
- [ ] 配置 tsconfig、vite.config
- [ ] Canvas 初始化 & 自适应窗口
- [ ] 绘制足球场地（草坪、边线、中线、中圈、球门）
- [ ] 绘制静态球员（圆形身体 + 朝向箭头 + 拖尾效果）和足球

### Phase 2 — 移动 & 输入
**目标**: 球员能跑起来
- [ ] 实现 InputManager 键盘输入系统
- [ ] 实现玩家 8 方向移动
- [ ] 球员朝向跟随移动方向旋转
- [ ] 跑动拖尾效果（速度越快拖尾越长）
- [ ] 球员边界约束（不能跑出场地）
- [ ] 足球基本物理（速度、摩擦力衰减）

### Phase 3 — 碰撞 & 踢球
**目标**: 能踢球了
- [ ] 球员-足球碰撞检测
- [ ] 自然带球（跑动碰到球 → 球被推动）
- [ ] 主动踢球（按键 → 球获得跑动速度叠加的冲量）
- [ ] 足球碰边界反弹
- [ ] 球员-球员碰撞（互相推开）
- [ ] 踢球冷却时间

### Phase 4 — 得分 & 游戏流程
**目标**: 完整的一局游戏
- [ ] 进球判定（球进入球门区域）
- [ ] 计分系统
- [ ] 进球后场地重置
- [ ] 胜利判定（可配置分数：3分/5分/10分/无限）
- [ ] 游戏状态机（menu → playing → goal → gameover）

### Phase 5 — UI 界面
**目标**: 看起来像个游戏
- [ ] 开始菜单（标题、按键说明、胜利分数选择、开始按钮）
- [ ] HUD 比分显示（显示当前胜利分数设置）
- [ ] 进球动画/提示
- [ ] 游戏结束画面（获胜方、重新开始）
- [ ] 暂停功能（P 键）

### Phase 6 — 打磨
**目标**: 体验更好
- [ ] 草坪条纹纹理
- [ ] 足球旋转动画
- [ ] 进球特效（粒子/闪光）
- [ ] 音效（踢球、进球、边线反弹）
- [ ] 手柄支持（可选）

### Phase 7 — 后端联机（扩展）
**目标**: 能联网对战
- [ ] Node.js + WebSocket 服务端
- [ ] 房间创建/加入
- [ ] 游戏状态同步
- [ ] 联机对战

---

## ❓ 待确认事项

| # | 问题 | 建议 | 状态 |
|---|------|------|------|
| 1 | 球门样式 | 凹槽式（球进入区域即得分） | ✅ 已确认 |
| 2 | 胜利分数 | 可选：3分/5分/10分/无限（默认5分） | ✅ 已确认 |
| 3 | 球员数量 | 1v1 先做 | 待确认 |
| 4 | 场地尺寸 | 自适应浏览器窗口 | 待确认 |
| 4 | 场地尺寸 | 自适应浏览器窗口 | 待确认 |

---

*确认计划后开始 Phase 1 ⚽*
